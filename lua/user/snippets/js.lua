return {
  s({
    trig = "clog",
    name = "console log",
    dscr = "The console.log() method outputs a message to the web console. The message may be a single string (with optional substitution values), or it may be any one or more JavaScript objects."
  },{
      t("console.log("),
      i(1),
      t(")"),
      i(0),
    })
}
